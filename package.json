{
  "name": "finance-bazar",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@9.8.1",
  "workspaces": [
    "apps/*",
    "backend"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
